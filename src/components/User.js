import React from "react";

// fetcher des users sur l'api,  sur l'iinterface de connection: créer compte: username password=> setLocalStorage.
// se connecter if username===login et user.password===password
export default function User() {
  return <div>User</div>;
}
